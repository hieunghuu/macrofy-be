# Imported by alembic/env.py so `Base.metadata` knows about every model.
# Every new model module must be imported here or Alembic won't detect it.

from app.db.session import Base  # noqa: F401
from app.models.meal import Meal  # noqa: F401
